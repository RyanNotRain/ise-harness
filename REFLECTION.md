# REFLECTION.md — 反思报告

## 1. 哪些技能真的有用

Superpowers 里对我帮助最大的是 brainstorming。刚开始我只想“做一个 coding agent harness”，它先问我六个维度里哪个要做深。我最后选了记忆。如果没被追问，我大概会把六部分各写一个很薄的类，表面齐全，实际没有重点。

writing-plans 也有用，但代码示例不能照单全收。Task 7 的 `defaultDeny` 没进入超时逻辑，subagent 原样复制后就成了死参数。PLAN 还用了 better-sqlite3 API，实际依赖却是 sql.js，冷启动 agent 编译时才发现冲突。

我没有按要求使用 worktree，也没有完成每个模块一个 MR。所有原始 commit 都在 main 上，这是明确的流程缺口，不能用“单人项目没必要”解释过去。分支隔离的价值不只在防止多人冲突，也在保留每个 task 的审查证据。

## 2. TDD 和测试质量

以前我觉得先写测试有些教条。这次最大的改观是：对 subagent 来说，测试不是验收单，而是它理解“什么算对”的最清楚上下文。Task 2 的正则状态 bug、Task 5 的 Float32Array/BLOB 重建错误，都靠测试和 review 才暴露。

但测试全绿也可能骗人。初版 code-index 测试对所有文本返回相同向量，只断言“查询有结果”，当然永远通过。初版护栏演示更典型：测试专用工具内部手工调用 Guard，生产 Agent 根本没有护栏。提交前审查时 53 个测试全绿，`npm run lint` 却失败，npm 包也没有可运行入口。整改后我增加了跨实例记忆恢复、主循环护栏、真实反馈回灌、供应商工具协议和 tarball 安装检查。现在我更在意测试是否穿过生产组装路径，而不是测试数量。

## 3. Subagent 的效率与盲点

Task 拆到“一到三个文件、一个明确行为”时，subagent 很快。太大容易迷路，太小又让 dispatch 和 review 的开销超过写代码本身。我的体感是 2–5 分钟的 task 最合适。

它的盲点也很固定：不质疑 brief。PLAN 写错，代码通常跟着错。Task 5 的 BLOB bug 和 Task 7 的超时逻辑都是这样。审查器能抓实现问题，却不一定指出设计漏项。`FileCredentialStore` 名字里有 File，内部却只是 Map；因为 PLAN 本身这么写，第一次 review 居然通过了。人仍要判断设计是否满足真实需求。

## 4. SPEC / PLAN 怎样影响实现

冷启动验证最有价值。Claude Code 只拿到 SPEC 和 PLAN，做 Task 4 时立刻问：依赖是 sql.js，示例却是 better-sqlite3，到底听哪个？这说明共享上下文会掩盖文档缺陷。后来我声称已经修订 SPEC，但最终审查发现技术选型表仍写着 better-sqlite3。这不是措辞问题，而是修订没有落到文件。

最终整改中，我把验收标准改成“实例 A 写盘关闭，实例 B 从同一路径恢复”。它能直接写成测试，不会被内存 Map 冒充。

## 5. Prompt 和上下文策略

最有效的做法是先给 task brief，再给它需要的接口，最后限制“只做 brief 中的内容”。背景塞得太多，subagent 会花时间理解无关模块。全局约束也不能太长。我保留的几条是 TDD、ESM、核心测试离线、不使用 agent 框架、凭据不得落入日志。

Review prompt 需要分两步：先看 spec 合规，再看代码质量。否则审查器会花很多篇幅讨论命名，却漏掉功能根本没接进主循环。

## 6. 凭据与分发

初版凭据实现是本项目最明显的侥幸：用内存 Map 加密，进程一关什么都没了，还用了默认主密码。反思里承认“没做好”不能替代修复。整改后改成 scrypt + AES-256-GCM 的磁盘密文，限制文件权限，主密码和 key 在 TTY 中不回显，并增加错误密码测试。

分发也一样。README 写 `npm install` 不代表包真的能装。整改后的 CI 会执行类型检查、构建、`npm pack`、安装 tarball 和 CLI 烟雾测试；我也用自己的账号完成了 npm 发布、PR 合并和 Pages 部署。文档里的 registry、CI 与 WebUI 地址都来自真实结果，而不是临时编写的占位符。

## 7. 如果重做

我会先验证依赖，再冻结 SPEC；每个模块从独立 worktree 开始，RED/GREEN 结果随 commit 或 MR 保存。Review 发现设计问题后，同步修改 PLAN，而不是只修源码。最后留一个全新的 agent 做提交前审查，因为这次最严重的问题不是某一行代码，而是“独立组件都有测试，完整产品却跑不起来”。

## 8. 对 Superpowers 的看法

Superpowers 的纪律有价值，但它默认 spec 足够可靠、subagent 能读出意图、review 能兜住问题。我的项目里这三点都不成立。spec 会过期，subagent 更相信示例代码，review 也可能接受“符合错误 PLAN”的实现。

这不说明流程没用，反而让我看清人的责任：决定做什么，检查验收条件是否代表真实需求，并在 AI 形成自洽循环时主动打断。最危险的不是测试失败，而是所有 agent 都同意、测试全绿，结果 key 存不住、护栏没接入、包也装不了。

## 学术规范声明

本文反思由学生 Ryan 本人撰写并确认，AI 只协助语言润色、错别字检查和结构整理；文中的判断、经历与最终表述由本人负责。
